import { Component, enableProdMode } from '@angular/core';
                  enableProdMode();
                  @Component({
                    selector: 'app-root',
                    template: '' + 
                      '<app-area></app-area>'
                  })
                  export class AppComponent { }